# Laravel-LEMP-Stack-Installation
Laravel LEMP Stack prepration on new linux machine

Clone the file to your newly created machine

make it execuable with `chmod +x armanstack.sh`

then run it `./armanstack.sh`
